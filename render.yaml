services:
  # Web Service: FastAPI API + Website pages
  - type: web
    name: market-lab-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: ENV
        value: production
      - key: MARKETLAB_API_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: market-lab-db
          property: connectionString

  # Cron Job: runs daily
  - type: cron
    name: market-lab-daily-update
    env: python
    schedule: "40 12 * * *" # 12:40 UTC = 18:10 IST
    buildCommand: pip install -r requirements.txt
    startCommand: python jobs/daily_update.py
    envVars:
      - key: ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: market-lab-db
          property: connectionString
      - key: MARKETLAB_API_KEY
        fromService:
          name: market-lab-web
          type: web
          envVarKey: MARKETLAB_API_KEY
      # Safer to set full URL manually later if needed
      - key: MARKETLAB_URL
        value: "https://market-lab-web.onrender.com"
      - key: TICKERS
        value: "AAPL,MSFT,TSLA"
      - key: PERIOD
        value: "30d"

databases:
  - name: market-lab-db
    plan: free
